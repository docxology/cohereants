# ant_stack

Directory `ant_stack` inside the `cohereants/src` tree of EntoTech.

Part of `cohereants/src` (EntoTech lane, local-only under `projects/ongoing/`).

## Contents
Files: `AGENTS.md`, `README.md`, `__init__.py`, `antbody.py`, `antbrain.py`, `antmind.py`

## Usage
- See `cohereants/src/README.md` for how this directory is produced and used.
