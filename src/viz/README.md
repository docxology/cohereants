# viz

Directory `viz` inside the `cohereants/src` tree of EntoTech.

Part of `cohereants/src` (EntoTech lane, local-only under `projects/ongoing/`).

## Contents
Files: `AGENTS.md`, `README.md`, `__init__.py`, `advanced.py`, `advanced_publication.py`, `advanced_summary.py`, `appendix_grid.py`, `behavioral_plots.py`, `figure_helpers.py`, `panels.py`, `plotly_helpers.py`, `styling.py`, `warnings_util.py`

## Usage
- See `cohereants/src/README.md` for how this directory is produced and used.
